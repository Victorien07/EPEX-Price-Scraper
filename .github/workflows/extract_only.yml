name: Extract EPEX data from HTML

on:
  schedule:
    - cron: "30 20 * * *"  # Tous les jours à 20h30 UTC
  workflow_dispatch:

jobs:
  extract:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Installer les dépendances
        run: pip install beautifulsoup4 pandas

      - name: Extraire depuis les fichiers HTML
        run: |
          python scripts/extract_prices.py

      - name: Commit du fichier CSV
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/epexspot_prices.csv
          git commit -m "📈 CSV mis à jour automatiquement" || echo "Pas de changement"
          git push
